# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Agent Backend (FastAPI)
# Example: http://localhost:8000 or https://<railway-app>.up.railway.app
VITE_AGENT_API_BASE=

# Edge Functions (Redis hazırlığı için)
# false: Direkt Supabase client kullan
# true veya boş: Edge function kullan (fallback var)
VITE_USE_EDGE_FUNCTIONS=true

# Keyword generation
# Opt-in: LLM ile keyword üretimi (Supabase Edge Function -> OpenAI) kullanılsın mı?
# false/boş: sadece deterministik fallback keyword üretimi (ücretsiz)
VITE_ENABLE_LLM_KEYWORDS=false
